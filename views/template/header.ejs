<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="Uyen Tam Tat - Deakin">
    <title>Task 10.4HD</title>
    <!-- My JS -->
    <script src="/formValidation.js"></script>
    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
</head>
<body style="padding: 0.5rem;">

<!-- Header -->
<header class="container-fluid">
    <!-- Top header -->
    <div class="row">
        <!-- Logo content -->
        <div class="col-md-2 col-xl-1 d-none d-md-block mt-2 ">
            <div class="d-flex p-2 justify-content-center">
                <a href="/"><img src="/images/icons/logo.png" width="100" alt="Main Logo"></a>
            </div>
        </div>
        <div class="col-md-9 col-xl-10 mt-2 p-2">
            <div class="row">
                <div class="col-md-12 text-center"><a href="/" style="text-decoration: none; color: black;"><h1>Deakin Art Supplies</h1></a></div>
                <div class="w-100"></div>
                <div class="col-md-12">
                    <form class="d-flex" role="search">
                        <input class="form-control me-1" type="search" placeholder="Search something...">
                        <button type="submit" class="btn btn-light border border-secondary-subtle"><i class="bi bi-search"></i></button>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-1 col-sm-6 d-flex align-items-center justify-content-center">
            <div class="d-grid w-100 h-75 my-auto">
                <a type="button" href="/user" class="btn btn-light border">
                    <i class="bi bi-person" style="font-size: x-large;"></i> <br>
                    <span >User</span>
                </a>
            </div>
        </div>
    </div>

    <!-- Navbar -->
    <nav class="rounded mt-2">
        <ul class="nav nav-pills nav-fill bg-info-subtle">
            <li class="nav-item">
              <a class="nav-link <% if (currentPage=="home") { %> active <% } else { %> text-secondary <% } %>" 
                href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <% if (currentPage=="categories") { %> active <% } else { %> text-secondary <% } %>" 
                href="/categories">Categories</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <% if (currentPage=="queryform") { %> active <% } else { %> text-secondary <% } %>" 
                href="/queryform">Query</a>
            </li>
        </ul>
    </nav>
</header>